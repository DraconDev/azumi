# Project State

## Current Focus
Enhanced documentation for safe `Raw()` usage in Azumi, with clearer warnings about CSS in `Raw()` and updated examples.

## Completed
- [x] Added explicit warning that CSS should never be used in `Raw()`
- [x] Updated example showing incorrect CSS usage in `Raw()`
- [x] Clarified safe usage cases for `Raw()` (trusted framework JS, session cleanup JS)
- [x] Improved documentation structure for `Raw()` safety guidelines
